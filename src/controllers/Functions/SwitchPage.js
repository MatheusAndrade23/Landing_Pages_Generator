import mockPage from '../../pages/mock.json'

const SwitchPage = (pageSelected) => {
  switch (pageSelected) {
    case 1:
      return (page = {});
      break;

    case 2:
      return (page = {});
      break;

    case 3:
      return (page = ...mockPage);
      break;

    case 4:
      return (page = {});
      break;

    case 5:
      return (page = {});
      break;

    case 6:
      return (page = {});
      break;

    case 7:
      return (page = {});
      break;

    case 8:
      return (page = {});
      break;

    case 9:
      return (page = {});
      break;
  }
};
